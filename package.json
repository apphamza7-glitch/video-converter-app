{
  "name": "video-converter-app",
  "version": "1.0.0",
  "description": "A simple starter project with:",
  "main": "index.js",
"scripts": {
  "server": "cd backend && node server.js",
  "client": "cd frontend && npm start",
  "dev": "cross-env NODE_ENV=development concurrently \"npm run server\" \"npm run client\"",
  "start": "cd backend && node server.js",
  "prebuild": "cd backend && npm install && cd ../frontend && npm install --include=dev",
  "build": "cd frontend && npm run build"
},
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "fluent-ffmpeg": "^2.1.3",
    "multer": "^2.2.0"
  },
  "devDependencies": {
    "concurrently": "^10.0.3",
    "cross-env": "^10.1.0"
  }
}
